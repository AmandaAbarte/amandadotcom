import React from "react";

export default function Contact (props) {
    return (
        <div className="contact-container" style={{ backgroundColor: props.lights ? "white" : "black" }}>
            <form className="form">

            </form>
        </div>
    )
}